<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN"
        "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html xmlns="http://www.w3.org/1999/xhtml">
<head>

 <%= stylesheet_link_tag "/report",:media=>"all" %>
 
  <style>
      /* PAGE, HEADERS, FOOTERS */
   .header { position: running(header) }
      @page {
          size: A4;
          border-top: 1px solid black;
          border-bottom: 1px solid black;
          margin: 18% 5% 6% 6%;
          counter-increment: page;
      @top-left {
      font-family : Arial, Helvetica, sans-serif;
          font-size: 11px;
          text-align: left;
          content:  element(header);
          vertical-align: bottom;
          padding: 3px;
          
      }
      @top-right {
        font-family : Arial, Helvetica, sans-serif;
        font-size: 11px;
        text-align: right;
        vertical-align: bottom;
        padding: 3px;
      }
      @bottom-left {
        font-family : Arial, Helvetica, sans-serif;
        font-size: 9px;
        content: "";
        vertical-align: top;
        padding: 3px;
      }
      @bottom-right {
        font-family : Arial, Helvetica, sans-serif;
        font-size: 12px;
        content: "Page " counter(page) " of " counter(pages);
        vertical-align: top;
        padding: 3px;
        text-align: right;
      }
      }

      /* BODY CSS */
     

  </style>
</head>
<body>
<div class="template1">
  <%= render :partial=>'users/pdf_header'%>
  <%= render :partial=>"users/pdf_summary" %>
  <%= render :partial=>"users/pdf_experience" %>
  <%= render :partial=>"users/pdf_projects" %>
  <%= render :partial=>"users/pdf_education" %>
</div>  
</body>
</html>
